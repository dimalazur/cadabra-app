import React, { Component } from 'react'
import classNames from 'classnames';
import PropTypes from 'prop-types'

class Button extends Component {

  render() {

    return (
      <button type="submit" class="btn">{'Button text'}</button>
    );
  }
}

export default Button;




